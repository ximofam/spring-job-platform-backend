services:
  postgres-db:
    image: postgres:16-alpine
    container_name: job_platform_postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: